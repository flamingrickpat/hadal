# ST-WI-03c

Story folder for the tier-3 predator roster, promoted from ST-03's work
items after a review split. WI-03c1 (tier-3 CreatureDefs, damage model,
and controllers) was further promoted to `ST-WI-03c1/`.

- `plan.md` — story node (YAML frontmatter, id `WI-03c`).
- `workitems/` — `WI-03c2.md` (landing the tier-3 predators in the
  production world).
