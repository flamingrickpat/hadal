{"status":"accepted","fingerprint":"6befbd94bc8b9f46ad5a2a6b97a367bcca741dea4a491539d6cde33be5f02deb","behavior_count":1,"subsystems":["audio system"],"coverage":true,"summary":"One focused behavior: finalize the per-band depth mixing (highCutoff roll-off, lowRumble, reverb, and other AudioProfile params where the settled band identity requires it) in the audio system's authored AUDIO_STOPS, applied through the existing AudioSystem.applyProfile ramp, with the section 43 master-volume seam kept intact. Single changed subsystem (audio system; src/util/audio.ts data seam, src/systems/AudioSystem.ts only via its existing path; src/render/band.ts explicitly read-only). Scope checks hold: 1 behavior, 1 changed subsystem. Dependencies WI-06a/b/c exist as ST-06 siblings, are neither ancestors nor descendants, and are the correct ordering basis (audio must match settled band identity). The single criterion AC-art-sound is retained verbatim; the depth part is owned here, the creature part by WI-06e-b, the music part and the final full-criterion proof by WI-06e-c, so the union covers the parent criterion. Verification is focused: Node monotonicity/interpolation test on AUDIO_STOPS, browser depth-ladder reading via the verified AudioSnapshot seam, section 34 node-count spot-check, suite/build green. Handoff names exact, verified file/symbol pointers (AUDIO_STOPS, audioProfileAtDepth, applyProfile, AudioSnapshot, setMasterVolume) and carries the user constraints (procedural WebAudio, budgets, spoiler rules, conservative-assumption rule for ambiguous parameters), so a fresh implementer can proceed."}
