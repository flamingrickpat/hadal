# Implementation Results

One file per implemented work item, written by the `item-implementer`
role:

- `WI-03b1-implementation.md` — the mid-depth useful/neutral fauna tier
  (request §11.1, §21): six `CreatureDef`s in `hiddenCreatures.ts` plus the
  simulation-side signature interactions (feeding trade, herding, node
  sweep, passive lift, chain ride, depth-tiered drift) in `Simulation.ts`,
  each proven headlessly by a per-species signature-rule scenario in
  `tier2Scenario.test.ts`, the 2+ friendly-interaction floor, and a
  tier-2 spoiler-containment sweep.
