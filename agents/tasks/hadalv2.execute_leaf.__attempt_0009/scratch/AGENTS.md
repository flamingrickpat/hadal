# Scratch — hadalv2.execute_leaf.__attempt_0009

Bounded implementer probes for WI-03a (tier-1 ambient/schooling fauna).

## implementation/

- `probe2.txt` .. `probe5.txt`, `t01-probe.txt`, `t13-probe.txt` — early
  headless scenario probes (vitest runs while the tier-1 tests were being
  shaped; superseded by the committed test suite).
- `AGENTS.md` — index for this folder.
- `browser-spot/` — the WI-03a browser spot-check probe.
