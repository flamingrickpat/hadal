# Scratch — implementation (WI-03a)

- `AGENTS.md` — this index.
- `probe2.txt` .. `probe5.txt`, `t01-probe.txt`, `t13-probe.txt` — early
  headless vitest runs while the tier-1 scenario tests were being shaped
  (superseded by `src/sim/tier1Scenario.test.ts`).
- `browser-spot/` — the browser spot-check probe (see its own AGENTS.md).
