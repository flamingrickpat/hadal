# WI-01 Re-verification — fresh workflow run (2026-09-06)

Role: `item-implementer`. The controller re-selected WI-01 in a fresh
workflow run (`state.md` fresh; accepted revision `2deabb0`). At that
revision the repository already contains the reviewer-approved WI-01
implementation (`184d348`), so this run re-ran every acceptance check
against the live repo and confirmed all criteria still hold. No product
code changed.

## Codegraph gate (re-run)

- `codegraph_explore` for boot/game symbols (projectPath `C:\Temp\hadal`)
  -> "No relevant code found". The `.codegraph/` index is stale for
  product symbols; per `ARCHITECTURE.md` and the WI-01 reviewer note,
  structural facts rest on direct file reads. Not a tooling blocker: the
  tool is present and working, the index simply has no product symbols.

## Commands re-run (2026-09-06)

| Command | Exit | Observed |
|---|---|---|
| `node --version` | 0 | v24.15.0 |
| `npm --version` | 0 | 11.12.1 |
| `npm install` | 0 | 0 vulnerabilities |
| `npm run build` | 0 | `tsc --noEmit && vite build`; 22 modules; `dist/` emitted; 555.74 kB three.js chunk notice (informational) |
| `npx vitest run` | 0 | 4 files / 31 tests pass (`terrain` 7, `PlayerController` 10, `PlayerMeters` 10, `rng` 4) |
| `git check-ignore -v design_private/probe` | 0 | `.gitignore:14:design_private/  design_private/probe` |
| `node scratch/item-implementer/WI-01/probe.mjs` | 0 | HADAL title; canvas; WebGL2; drawing buffer 1920x1080; non-blank frame (luminance range 210); zero page exceptions; zero console errors |

## Browser boot detail (probe re-run)

`output/result.json` checks all true:

- page title is HADAL
- canvas present
- WebGL2 context on canvas
- drawing buffer is 1920x1080 (dpr 1)
- frame is not blank (pixel luminance range > 10; observed range 210)
- no page exceptions
- no console errors

`output/console.json`: two `[vite]` debug lines and one benign
"GPU stall due to ReadPixels" warning from the probe's own same-frame
`readPixels` under SwiftShader. No `error`-type console messages, no page
exceptions. `output/boot-1920x1080.png` shows the player capsule + aim
line + HUD (O2 180s / HP 100 / DEPTH 100m / TOOL Salvage Knife) on the
dark scene background.

## Result

All nine WI-01 acceptance criteria **passed** at `2deabb0`. Result:
implemented (re-verified). See `../../implementation/WI-01-implementation.md`
("Re-verification" section) for the full acceptance table.
