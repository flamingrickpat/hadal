# Browser Verification — WI-06g

- probe.mjs — Playwright Chromium verification of accessibility controls: opens settings overlay, toggles each control, verifies save persistence through reload.
- output/ — probe output artifacts

Run: `node probe.mjs` (requires dev server on port 5500)
