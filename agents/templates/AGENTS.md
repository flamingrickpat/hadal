# Templates

`architecture_inspector.html` is the shared version-2 architecture viewer.
`mermaid.min.js` is vendored beside generated inspectors.

Only the roles named by the architecture-preview instructions may create an
`inspector.html`.
